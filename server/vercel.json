{
  "version": 2,
  "builds": [
    {
      "src": "client/*",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/build"
      }
    },
    {
      "src": "server/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "client/build/$1"
    },
    {
      "src": "/api/(.*)",
      "dest": "server/$1"
    }
  ]
}
